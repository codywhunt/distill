name: distill_canvas
description: Distill Canvas - A high-performance, infinite pannable/zoomable canvas for Flutter. Supports layered rendering, world/view coordinate conversion, animated camera, and gesture handling.
version: 0.1.0
publish_to: none

environment:
  sdk: ">=3.7.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  collection: ^1.19.1

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
